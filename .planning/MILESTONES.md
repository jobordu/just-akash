# Milestones

## v1.4 — Secrets, Exec & Unified CLI

**Shipped:** 2026-04-18
**Phases:** 1–5 (estimated, retroactively assigned)

### What shipped
- Secrets injection via SSH (`just inject` / `just-akash inject`)
- Remote command execution (`just exec` / `just-akash exec`)
- SDL env injection at deploy time (`--env KEY=VALUE`)
- Configurable Console API URL via `AKASH_CONSOLE_URL`
- Unified CLI (all commands top-level: deploy, connect, exec, inject, list, status, destroy, tag)
- E2E secrets test (`just test-secrets`)
- 357 tests, 69% coverage

### Last phase number: 5
