---
phase: 09-interactive-shell
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_interactive_shell.py
autonomous: true
requirements: [SHLL-01, SHLL-02, SHLL-03, SHLL-04]
formal_artifacts: none

must_haves:
  truths:
    - "tests/test_interactive_shell.py exists with named test stubs for all four SHLL requirements"
    - "Running pytest tests/test_interactive_shell.py yields FAILED (not error), confirming stubs fail before implementation"
  artifacts:
    - path: "tests/test_interactive_shell.py"
      provides: "Wave 0 failing test stubs covering SHLL-01 through SHLL-04"
      min_lines: 60
  key_links:
    - from: "tests/test_interactive_shell.py"
      to: "just_akash/transport/lease_shell.py"
      via: "import LeaseShellTransport; calls connect() which currently raises NotImplementedError"
      pattern: "LeaseShellTransport"
---

<objective>
Create Wave 0 test scaffolding for Phase 9 (Interactive Shell) before any implementation begins.

Purpose: Nyquist compliance — failing tests must exist before implementation so executor can verify RED→GREEN progression. Tests also act as a precise specification for connect() behavior.
Output: tests/test_interactive_shell.py with 12 failing stubs covering TTY setup, terminal dimensions, SIGINT forwarding, SIGWINCH resize, bidirectional I/O dispatch, and terminal restoration.
</objective>

<execution_context>
@/Users/jonathanborduas/.claude/nf/workflows/execute-plan.md
@/Users/jonathanborduas/.claude/nf/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-interactive-shell/09-RESEARCH.md
@just_akash/transport/lease_shell.py
@just_akash/transport/base.py
@tests/test_transport.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_interactive_shell.py with failing stubs</name>
  <files>tests/test_interactive_shell.py</files>
  <action>
Create tests/test_interactive_shell.py with 12 failing stub tests covering all SHLL requirements. Each test must:
- Import from just_akash.transport.lease_shell and just_akash.transport.base
- Use a helper _make_transport() that creates a LeaseShellTransport with a pre-set _ws_url and _service (avoids needing prepare() to succeed)
- Patch termios, tty, signal, os.get_terminal_size, and websockets.sync.client.connect where needed
- Be a proper test that FAILS right now because connect() raises NotImplementedError

Required test class: TestLeaseShellConnect

Required tests (use exact names — VALIDATION.md references tests/test_interactive_shell.py):

1. test_connect_opens_websocket_with_tty_true_stdin_true
   Assert: WebSocket URL has "tty=true" and "stdin=true" query params

2. test_connect_sends_terminal_size_on_connect
   Assert: ws.send() called with a bytes frame where frame[0] == 105 (resize frame code)
   Use os.terminal_size patch: return os.terminal_size((80, 24))

3. test_connect_forwards_stdin_to_frame_104
   Assert: stdin input "hello" is forwarded as bytes([104]) + b"hello"

4. test_connect_dispatches_frame_100_to_stdout
   Assert: receiving frame bytes([100]) + b"output" writes b"output" to sys.stdout.buffer

5. test_connect_dispatches_frame_101_to_stderr
   Assert: receiving frame bytes([101]) + b"err" writes b"err" to sys.stderr.buffer

6. test_connect_exits_on_frame_102
   Assert: connect() returns (does not block) when frame code 102 is received

7. test_sigint_sends_frame_104_with_0x03
   Assert: After SIGINT handler installed, calling the handler sends bytes([104, 0x03]) via ws.send()

8. test_sigint_does_not_raise_keyboardinterrupt
   Assert: Sending SIGINT during connect() does not raise KeyboardInterrupt in the caller

9. test_sigwinch_sends_frame_105_with_new_size
   Assert: After SIGWINCH handler installed, calling the handler sends frame where frame[0] == 105 and struct.unpack('>HH', frame[1:5]) matches new terminal size

10. test_terminal_restored_on_normal_exit
    Assert: termios.tcsetattr() is called at least once after _run_interactive_session completes normally

11. test_terminal_restored_on_exception
    Assert: Even when _run_interactive_session raises RuntimeError("crash"), termios.tcsetattr() is still called (finally block runs)

12. test_terminal_restored_on_connection_close
    Assert: termios.tcsetattr() is called after ConnectionClosedOK is raised during the session

Implementation notes:
- Use a _make_transport() helper factory pattern (see 09-RESEARCH.md > Test Examples section)
- DEPLOYMENT_FIXTURE should match the shape in existing test files: {"leases": [{"provider": {"hostUri": "https://provider.us-east.akash.pub:8443"}, "status": {"services": {"web": {}}}}]}
- Import websockets.exceptions.ConnectionClosedOK for connection close tests
- Import struct for frame structure assertions
- For stub tests that cannot fully exercise the logic yet (e.g., bidirectional I/O tests), mark with: pytest.mark.xfail(reason="Wave 0 stub: implementation not yet present") OR write them as assertion-based checks on constants/frame structures that will pass once connect() is real
- Tests 10, 11, 12 (terminal restore) MUST be genuine failing tests right now — use patch('termios.tcgetattr') + patch('termios.tcsetattr') as mock and assert mock_tcsetattr.called after connect() which currently raises NotImplementedError before ever calling tcsetattr
  
IMPORTANT: Right now connect() raises NotImplementedError. Tests 10-12 should patch the NotImplementedError away (patch connect's internal path) OR use pytest.raises(NotImplementedError) but then check that a spy on tcsetattr was called — the latter won't work since tcsetattr never runs. Instead: write these tests as TODO stubs with a single assert False / pytest.fail("not yet implemented") body so they fail RED correctly and will be fixed during Task 1 of Plan 02.
  </action>
  <verify>
Pre-flight: confirm tests/ directory exists — ls tests/test_transport.py should return the file.

After writing: run pytest tests/test_interactive_shell.py --tb=short -q

Expected output: 12 tests collected, all FAILED (F) or ERROR (E) — NOT 0 collected. If 0 collected, test naming is broken.
  </verify>
  <done>
tests/test_interactive_shell.py exists with 12 tests. pytest tests/test_interactive_shell.py collects all 12 and they all fail (FAILED or ERROR status). No tests pass — they must fail to be genuine Wave 0 stubs.
  </done>
</task>

</tasks>

<verification>
Run: pytest tests/test_interactive_shell.py --tb=short -q
Expected: 12 tests FAILED or ERROR, 0 passed.
Run: pytest tests/ --tb=short -q
Expected: overall test count increases by exactly 12; existing tests still pass (no regressions).
</verification>

<success_criteria>
- tests/test_interactive_shell.py exists and is importable
- pytest collects exactly 12 tests from the file
- All 12 fail (RED) — confirmed by pytest exit code 1
- No existing tests broken by the new file
</success_criteria>

<output>
After completion, create .planning/phases/09-interactive-shell/09-01-SUMMARY.md
</output>
